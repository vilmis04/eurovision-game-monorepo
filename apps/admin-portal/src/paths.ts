enum RootPaths {
  HOME = '/home',
  LOGIN = '/login',
  CONFIG = '/config',
  COUNTRIES = '/countries',
}

export const paths = {
  home: RootPaths.HOME,
  login: RootPaths.LOGIN,
  config: RootPaths.CONFIG,
  countries: RootPaths.COUNTRIES,
};
