export enum RootPaths {
	VOTES = "votes",
	ADMIN = "admin",
	SCORES = "scores",
	GROUPS = "groups",
}

export enum AuthPaths {
	AUTH = "auth",
	LOGIN = "login",
	LOGOUT = "logout",
	SIGN_UP = "signUp",
	ROLES = "roles",
}

export enum AdminPaths {
	PROMOTE = "promote",
	SUBMIT_FINAL = "submit-final",
}
